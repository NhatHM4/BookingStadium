package com.booking.stadium.telegram.lead;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TelegramBookingLeadRepository extends JpaRepository<TelegramBookingLead, Long> {
}
